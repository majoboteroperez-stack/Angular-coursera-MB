version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:18.16.0
    steps:
      - checkout
      - run: npm install
      - run: npm run build
      - run: npm test #: Aquí se correrían las pruebas de Jasmine y Cypress
